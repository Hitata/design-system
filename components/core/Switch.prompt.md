**Switch** — a square toggle; works controlled or uncontrolled.

```jsx
<Switch label="Dark ground" defaultChecked={false} onChange={(v) => {}} />
<Switch checked={on} onChange={setOn} label="Expose grid" />
```

Hard ink thumb slides with no easing flourish; track fills ember when on. Square corners, hairline frame — no rounding.
